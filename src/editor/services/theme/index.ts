import store from "../../store"

store.registerModule("$theme", {
    state: {
        "now": "Default Dark+"
    },
    mutations: {
        changeTheme(state, id: string) {
            
        }
    }
})